# NBA Data Project

## Current milestone

Bootstrap the project before implementing data ingestion.

## Verified repository state

- Python 3.12 virtual environment exists.
- `src/main.py` is the only application module and currently prints a startup message.
- No data source, dependency manifest, database schema, pipeline script, or generated output exists yet.
- `PROJECT_CONTEXT.md` records the latest inspection and the next required decision.

## Scope for the next implementation step

Choose the authoritative NBA data source and define the first persisted dataset's schema. The ingestion script should be added only after those choices are documented.
